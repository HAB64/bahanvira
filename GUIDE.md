# 📖 راهنمای مدیریت سایت بهان رایانه

## 🔙 چگونه تغییرات خراب‌کننده را برگردانیم؟

### روش ۱: بازگشت به نسخه پایدار (ساده‌ترین)
```bash
cd /home/z/my-project
./rollback.sh
```
این اسکریپت آخرین نسخه پایدار برچسب‌خورده را بازیابی می‌کند.

### روش ۲: بازگشت به برچسب خاص
```bash
./rollback.sh v-stable-2026-06-12
```

### روش ۳: بازگشت به یک commit قبل
```bash
# دیدن تاریخچه تغییرات
git log --oneline -10

# بازگشت به commit خاص
git checkout abc1234 -- .

# یا بازگشت به یک commit قبل
git checkout HEAD~1 -- .
```

---

## 🏷️ چگونه قبل از تغییرات مهم، نسخه پایدار ذخیره کنیم؟

### قبل از هر تغییر بزرگ:
```bash
# برچسب زدن به نسخه فعلی
git tag -a v-stable-YYYY-MM-DD -m "توضیح: مثلاً قبل از تغییر رنگ‌ها"
git push origin --tags
```

### مثال:
```bash
git tag -a v-stable-2026-06-13 -m "قبل از اضافه کردن سیستم CRM"
git push origin --tags
```

---

## 🚀 دیپلوی سایت

### دیپلوی معمولی:
```bash
cd /home/z/my-project
GITHUB_TOKEN=توکن_شما ./deploy.sh "پیام commit"
```

### فقط بیلد (بدون دیپلوی):
```bash
npm run build
```

---

## 📋 روند کار اصولی (Best Practice)

```
۱. برچسب زدن به نسخه فعلی    →  git tag -a v-stable-... -m "..."
۲. اعمال تغییرات              →  ویرایش فایل‌ها
۳. تست محلی                   →  npm run build
۴. در صورت موفقیت:            →  commit + tag + deploy
۵. در صورت خرابی:             →  ./rollback.sh
```

---

## ⚡ دستورات سریع

| کار | دستور |
|-----|-------|
| دیدن تاریخچه | `git log --oneline -10` |
| دیدن برچسب‌ها | `git tag -l "v-stable*"` |
| ذخیره نسخه پایدار | `git tag -a v-stable-DATE -m "توضیح"` |
| بازگشت به نسخه پایدار | `./rollback.sh` |
| دیپلوی | `GITHUB_TOKEN=... ./deploy.sh` |
| بیلد تستی | `npm run build` |

---

## 🛡️ نکات مهم

1. **هرگز توکن را مستقیم در کد ننویسید** - از متغیر محیطی GITHUB_TOKEN استفاده کنید
2. **قبل از هر تغییر بزرگ، نسخه پایدار را برچسب بزنید**
3. **deploy.sh به‌طور خودکار:**
   - بیلد می‌کند
   - مسیر تصاویر را اصلاح می‌کند
   - فایل .nojekyll را اضافه می‌کند
   - به GitHub Pages دیپلوی می‌کند
4. **rollback.sh قبل از بازگشت، وضعیت فعلی را در شاخه پشتیبان ذخیره می‌کند**
